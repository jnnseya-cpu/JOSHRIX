{
  "name": "joshrix-functions",
  "private": true,
  "description": "JOSHRIX Studio backend — Firebase Cloud Functions (2nd gen)",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "copy-shared": "node -e \"require('fs').cpSync('../shared', 'src/shared', {recursive: true})\"",
    "build": "npm run copy-shared && tsc"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "firebase-functions": "^7.3.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
